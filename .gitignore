node_modules/
.DS_Store
*.log
.env
.env.local
dist/
.idea/
.vscode/
# Persisted game state (rounds, GGR, history). Generated at runtime;
# never commit it — it'd overwrite production state on every deploy.
server/.state/
